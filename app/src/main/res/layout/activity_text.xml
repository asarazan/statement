<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:visibility="visible">

    <net.sarazan.statement.FontFitEditText
        android:id="@android:id/edit"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@null"
        android:textSize="72sp"
        android:textColor="@color/white"
        android:gravity="top|left"
        android:layout_margin="@dimen/margin_large"
        android:imeOptions="actionDone"
        android:focusable="true"
        android:singleLine="false"
        android:textColorHint="@color/white_alpha"
        android:hint="Begin typing or tap to edit"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_large"
        android:src="@mipmap/ic_done_black"
        app:elevation="@dimen/elevation_normal"
        app:pressedTranslationZ="@dimen/elevation_high"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        app:backgroundTint="@color/app_green"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_large"
        android:src="@mipmap/ic_delete_black"
        app:elevation="@dimen/elevation_normal"
        app:pressedTranslationZ="@dimen/elevation_high"
        android:layout_alignParentBottom="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toLeftOf="@+id/done"
        app:backgroundTint="@color/app_pink"/>

</RelativeLayout>