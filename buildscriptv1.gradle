ext {
    sdk_version     = 22
    kotlin_version  = "0.12.613"
    build_tools     = "22.0.1"
    gradle_plugin   = "1.3.0-beta4"
    support_version = "22.2.0"
    play_version    = "7.5.0"
    propertyOrEnv = { key -> project.hasProperty(key) ? project.getProperty(key) : System.getenv(key) }
}
